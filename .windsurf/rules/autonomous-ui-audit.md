<rule>
  <description>Autonomous UI/UX Auditing and Self-Correction Protocol</description>
  <guidelines>
    - Proactive Upgrades: Whenever you open or modify a file, autonomously audit its current UI/UX. If you detect legacy styling (standard generic Tailwind colors, basic buttons, default borders), you have permission to automatically upgrade it to the "Digital Twilight" standard (deep blacks, `border-white/10`, `backdrop-blur-xl`, and `#00F2FF` cyan accents).
    - Injecting "Bells and Whistles": Proactively add 2026-tier micro-interactions without being explicitly asked. For example: convert static buttons into magnetic hover elements, add `group-hover` border glows to cards, and apply staggered GSAP fade-ins to long lists or text blocks.
    - Ruthless Consistency: If you spot any text missing the strict typography system (e.g., a standard sans-serif instead of `Geist`, or a basic serif instead of `Playfair_Display italic`), fix it immediately to enforce brand alignment.
    - UX Polish: Automatically remove default browser focus rings and replace them with custom `:focus-visible` cyan glows. Ensure custom scrollbars (if Lenis is bypassed) match the dark theme. Never leave a raw native HTML element unstyled.
  </guidelines>
</rule>