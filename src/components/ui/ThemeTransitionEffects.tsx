/**
 * Theme Transition Effects
 * 
 * This component previously showed special effects when switching between brand modes.
 * Since brand switching has been removed (only 'default' brand is supported),
 * this component now returns null.
 * 
 * @deprecated Brand switching is no longer supported. This component is a no-op.
 */
export default function ThemeTransitionEffects() {
  // Brand switching has been removed, so no transition effects are needed
  return null;
}

